<div align="center">

# 🤖 CCB Gateway

### 企业级多 AI 编排平台

[![Stars](https://img.shields.io/github/stars/LeoLin990405/ai-router-ccb?style=social)](https://github.com/LeoLin990405/ai-router-ccb)
[![License](https://img.shields.io/github/license/LeoLin990405/ai-router-ccb?color=blue)](LICENSE)
[![Python](https://img.shields.io/badge/Python-3.9+-3776AB?logo=python&logoColor=white)](https://www.python.org)
[![FastAPI](https://img.shields.io/badge/FastAPI-009688?logo=fastapi&logoColor=white)](https://fastapi.tiangolo.com)
[![Version](https://img.shields.io/badge/version-0.19--alpha-brightgreen)](https://github.com/LeoLin990405/ai-router-ccb/releases)

**Claude 通过统一的 Gateway API 编排 8 个 AI Provider，自动注入记忆和实时监控**

[功能特性](#-功能特性) • [快速开始](#-快速开始) • [使用文档](#-使用文档) • [系统架构](#-系统架构) • [API 参考](#-api-参考)

[🇺🇸 English](README.md) | [🇨🇳 简体中文](README.zh-CN.md)

<img src="screenshots/webui-demo.gif" alt="CCB Gateway 演示" width="800">

</div>

---

## 📖 目录

- [概述](#-概述)
- [为什么选择 CCB Gateway](#-为什么选择-ccb-gateway)
- [功能特性](#-功能特性)
- [系统架构](#-系统架构)
- [快速开始](#-快速开始)
- [使用文档](#-使用文档)
- [记忆系统](#-记忆系统-v018)
- [技能发现](#-技能发现-v019)
- [多 AI 讨论](#-多-ai-讨论)
- [Web UI](#-web-ui)
- [API 参考](#-api-参考)
- [文档资源](#-文档资源)
- [开发路线](#-开发路线)
- [贡献指南](#-贡献指南)
- [开源许可](#-开源许可)

---

## 🌟 概述

**CCB Gateway** 是生产级的多 AI 编排平台，**Claude 作为智能编排者**，通过统一的 Gateway API 将任务路由到 8 个专业 AI Provider，提供自动记忆、缓存、重试和实时监控功能。

**独特优势：**
- 🧠 **自动记忆** - 每次对话自动记录，相关上下文自动注入
- 🎯 **预加载上下文** - 53 个 Skills + 8 个 Providers + 4 个 MCP Servers 嵌入每个请求
- 🔍 **技能发现** - 通过 Vercel Skills CLI 自动发现和推荐相关技能
- ⚡ **智能路由** - 基于速度分级的自动降级和智能 Provider 选择
- 📊 **实时监控** - 基于 WebSocket 的仪表盘和实时指标
- 🔄 **多 AI 讨论** - 多个 AI 协作解决问题
- ☁️ **云端同步** - Google Drive 备份，每小时自动同步

```
                    ┌─────────────────────────────┐
                    │   Claude (Orchestrator)     │
                    │      Claude Code CLI        │
                    └─────────────┬───────────────┘
                                  │
              ┌───────────────────┼───────────────────┐
              │                   │                   │
    ┌─────────▼─────────┐ ┌──────▼──────┐ ┌─────────▼─────────┐
    │   ccb-cli         │ │ Gateway API │ │   Web UI          │
    │  直接调用         │ │  REST/WS    │ │   仪表盘          │
    └─────────┬─────────┘ └──────┬──────┘ └─────────┬─────────┘
              │                  │                   │
              └──────────────────┼───────────────────┘
                                 │
          ┌──────────┬───────────┼───────────┬───────────┬─────────┐
          ▼          ▼           ▼           ▼           ▼         ▼
     ┌────────┐ ┌────────┐ ┌─────────┐ ┌────────┐ ┌────────┐ ┌────────┐
     │ Kimi   │ │ Qwen   │ │DeepSeek │ │ Codex  │ │Gemini  │ │ iFlow  │
     │ 🚀 7s  │ │ 🚀 12s │ │ ⚡ 16s  │ │ 🐢 48s │ │ 🐢 71s │ │ ⚡ 25s │
     └────────┘ └────────┘ └─────────┘ └────────┘ └────────┘ └────────┘
                           ┌─────────┐ ┌─────────┐
                           │ Qoder   │ │OpenCode │
                           │ ⚡ 30s  │ │ ⚡ 42s  │
                           └─────────┘ └─────────┘
```

---

## 💡 为什么选择 CCB Gateway

<table>
<tr>
<td width="50%">

### 问题

❌ 多个 AI CLI 接口不统一
❌ 手动选择 Provider 繁琐
❌ 对话之间没有记忆
❌ 上下文丢失，AI 不知道可用工具
❌ 无法观察操作过程
❌ AI 之间无法协作
❌ 失败请求浪费时间

</td>
<td width="50%">

### 解决方案

✅ **统一 Gateway API** - 一个接口调用所有
✅ **智能路由** - 自动选择最佳 AI
✅ **自动记忆** - 上下文自动保留
✅ **预加载工具** - 53 个 Skills 嵌入
✅ **实时仪表盘** - 完全可观测
✅ **多 AI 讨论** - 协作式 AI
✅ **重试与降级** - 内置弹性

</td>
</tr>
</table>

---

## ✨ 功能特性

### 🧠 自动记忆系统 (v0.18)

**零配置记忆** - 每次对话自动记录，相关上下文自动注入。

<details>
<summary><b>预加载上下文（点击展开）</b></summary>

每个请求自动包含：
- 🎯 **53 个 Claude Code Skills** - frontend-design, pdf, xlsx, pptx, ccb, lenny-* 等
- 🔌 **4 个 MCP Servers** - chroma-mcp, playwright-mcp 等
- 🤖 **8 个 AI Providers** - 模型、优势、使用场景
- 💭 **相关记忆** - 通过 FTS5 全文搜索检索历史对话

**性能：**
- ⚡ 每个请求 <100ms 开销（<5% 影响）
- 📝 100% 对话捕获率
- 🔍 ~80% 搜索准确率（未来向量搜索可达 90%+）

</details>

<details>
<summary><b>记忆后端</b></summary>

- 💾 **SQLite 存储** - 所有对话存储在 `~/.ccb/ccb_memory.db`
- 🔎 **全文搜索** - FTS5 支持中文
- ☁️ **云端同步** - Google Drive 备份（每小时自动同步）
- 📊 **分析统计** - 追踪每个 AI 擅长的任务类型

</details>

<details>
<summary><b>自动集成</b></summary>

**请求前钩子：**
```python
# 调用 AI Provider 之前
request = await memory_middleware.pre_request(request)
# 结果：消息增强了系统上下文 + 相关记忆
```

**响应后钩子：**
```python
# AI 响应后
await memory_middleware.post_response(request, response)
# 结果：对话自动保存到数据库
```

</details>

**使用方式：**
```bash
# 无需特殊命令 - ccb-cli 现在自动拥有记忆！
ccb-cli kimi "如何构建登录页面？"

# [Gateway Middleware]
#   ✓ 系统上下文已注入（53 Skills + 4 MCP + 8 Providers）
#   ✓ 已注入 2 条相关记忆
#
# 响应：基于之前关于 React 的讨论...
#
# 💡 [已自动注入 2 条相关记忆]

# 查看记忆
python3 lib/memory/memory_lite.py recent 10
python3 lib/memory/memory_lite.py search "React"
```

---

### ⚡ 智能路由与降级

**基于速度分级的 Provider 链**，失败时自动降级：

```yaml
快速层（3-15秒）：   Kimi → Qwen → DeepSeek
中速层（15-45秒）：  iFlow → Qoder → OpenCode
慢速层（45-90秒）：  Codex → Gemini
```

**功能特性：**
- 🎯 基于任务关键词的智能 Provider 推荐
- 🔄 指数退避的自动重试
- 📉 弹性降级链
- ⚖️ 跨 Provider 负载均衡

---

### 🤝 多 AI 讨论

**协作式问题解决** - 多个 AI 讨论并达成共识：

```bash
# 启动 3 个 AI 的讨论
ccb-submit discuss \
  --providers kimi,codex,gemini \
  --rounds 3 \
  --strategy "consensus" \
  "设计可扩展的微服务架构"

# 每个 AI：
# 第 1 轮：提出初始方案
# 第 2 轮：评审其他方案
# 第 3 轮：最终推荐

# 输出：综合所有视角的解决方案
```

**使用场景：**
- 🏗️ 架构设计
- 🐛 复杂调试
- 📝 技术文档
- 💡 头脑风暴

---

### 📊 实时监控

**基于 WebSocket 的仪表盘**，实时更新：

<table>
<tr>
<td width="33%">

**指标**
- 请求数量
- 成功率
- 平均延迟
- Provider 状态

</td>
<td width="33%">

**队列**
- 待处理请求
- 处理中
- 已完成
- 失败

</td>
<td width="33%">

**日志**
- 实时事件
- 错误追踪
- 性能数据
- WebSocket 推送

</td>
</tr>
</table>

访问地址：http://localhost:8765/web

---

### 🚀 生产级功能

<table>
<tr>
<td width="50%">

**性能**
- ⚡ 响应缓存（可配置 TTL）
- 🔄 指数退避重试
- 📊 每个 Provider 的速率限制
- 🎯 并行执行

</td>
<td width="50%">

**可靠性**
- 🛡️ 自动降级链
- 💾 持久化请求队列
- 📝 完整日志
- 🔍 基于 ID 的请求追踪

</td>
</tr>
<tr>
<td width="50%">

**安全性**
- 🔐 API Key 认证
- 🚦 速率限制
- 🔒 安全的凭据存储
- 📋 审计日志

</td>
<td width="50%">

**可观测性**
- 📊 Prometheus 指标
- 📈 实时仪表盘
- 🔔 WebSocket 事件
- 📋 详细请求日志

</td>
</tr>
</table>

---

## 🏗️ 系统架构

### 系统概览

```
┌─────────────────────────────────────────────────────────────────┐
│                      CCB Gateway (v0.18)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────────────────────────────────────────┐    │
│  │            记忆中间件 (v0.18)                           │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  请求前钩子：                                           │    │
│  │  • SystemContextBuilder (53 Skills + 4 MCP + 8 Prov)   │    │
│  │  • MemoryLite.search() → FTS5 搜索                     │    │
│  │  • Provider 推荐                                        │    │
│  │  • 上下文注入                                           │    │
│  │                                                          │    │
│  │  响应后钩子：                                           │    │
│  │  • MemoryLite.record() → SQLite                        │    │
│  │  • 更新统计数据                                         │    │
│  └────────────────────────────────────────────────────────┘    │
│                          │                                       │
│  ┌────────────────────────▼─────────────────────────────┐      │
│  │            Gateway Server 核心                        │      │
│  ├───────────────────────────────────────────────────────┤      │
│  │  • 请求队列（异步）                                   │      │
│  │  • 重试执行器                                         │      │
│  │  • 缓存管理器                                         │      │
│  │  • 速率限制器                                         │      │
│  │  • 指标收集器                                         │      │
│  └───────────────────────────────────────────────────────┘      │
│                          │                                       │
│  ┌───────────┬───────────┼───────────┬───────────┐            │
│  ▼           ▼           ▼           ▼           ▼            │
│ ┌─────┐   ┌─────┐   ┌─────────┐  ┌─────┐   ┌───────┐        │
│ │Kimi │   │Qwen │   │DeepSeek │  │Codex│   │Gemini │   ...  │
│ └─────┘   └─────┘   └─────────┘  └─────┘   └───────┘        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 记忆系统流程

```
用户请求
    │
    ├─→ [记忆中间件：请求前]
    │   ├─→ 从消息中提取关键词
    │   ├─→ SystemContextBuilder.get_relevant_context()
    │   │   ├─→ 按关键词过滤 53 个 Skills
    │   │   ├─→ 获取当前 Provider 信息
    │   │   └─→ 获取活动的 MCP Servers
    │   ├─→ MemoryLite.search_conversations()
    │   │   └─→ SQLite FTS5 全文搜索
    │   └─→ 注入到提示词：
    │       """
    │       # 系统上下文
    │       ## 🤖 当前 Provider: kimi
    │       ## 🛠️ 相关 Skills: frontend-design, pptx
    │       ## 💭 相关记忆: [历史对话]
    │
    │       ---
    │       # 用户请求
    │       [原始消息]
    │       """
    │
    ├─→ [Provider 调用]
    │   └─→ 增强后的消息 → AI Provider
    │
    └─→ [记忆中间件：响应后]
        ├─→ 记录对话到 SQLite
        ├─→ 更新 FTS5 索引
        └─→ 更新 Provider 统计
```

---

## 🚀 快速开始

### 前置条件

- Python 3.9+
- Node.js 16+（用于 MCP servers）
- Git

### 安装

```bash
# 1. 克隆仓库
git clone https://github.com/LeoLin990405/ai-router-ccb.git
cd ai-router-ccb

# 2. 安装 Python 依赖
pip install -r requirements.txt

# 3. 安装 Node.js 依赖（用于 MCP）
npm install

# 4. 配置 AI Providers
# 编辑 ~/.claude/ 中的配置文件或使用环境变量
```

### 启动 Gateway Server

```bash
# 使用默认配置启动
python3 -m lib.gateway.gateway_server --port 8765

# 使用自定义配置
python3 -m lib.gateway.gateway_server --config config/gateway.yaml

# 输出：
# [SystemContext] Preloading system information...
# [SystemContext] Loaded 53 skills
# [SystemContext] Loaded 8 providers
# [SystemContext] Loaded 4 MCP servers
# [MemoryMiddleware] Initialized (enabled=True)
# [GatewayServer] Memory Middleware initialized successfully
# ✓ Server running at http://localhost:8765
```

### 第一个请求

```bash
# 使用 ccb-cli（自动记忆！）
ccb-cli kimi "解释 React Hooks"

# 使用 curl
curl -X POST http://localhost:8765/api/ask \
  -H "Content-Type: application/json" \
  -d '{
    "provider": "kimi",
    "message": "解释 React Hooks",
    "wait": true,
    "timeout": 60
  }'

# 响应包括：
# - AI 响应
# - 注入的上下文元数据
# - 延迟指标
```

---

## 📚 使用文档

### ccb-cli - 直接 CLI

**调用任何 AI Provider 的最快方式：**

```bash
# 基本用法
ccb-cli <provider> [model] "<message>"

# 示例
ccb-cli kimi "如何优化 SQL 查询？"
ccb-cli codex o3 "证明停机问题不可判定"
ccb-cli gemini 3f "设计响应式导航栏"
ccb-cli qwen "分析这个数据框"

# 使用 Agent 角色
ccb-cli codex o3 -a reviewer "审查这个 PR"
ccb-cli kimi -a sisyphus "修复这个 bug: ..."
```

**模型快捷方式：**
| Provider | 快捷方式 | 示例 |
|----------|-----------|---------|
| codex | o3, o4-mini, gpt-4o, o1-pro | `ccb-cli codex o3 "..."` |
| gemini | 3f, 3p, 2.5f, 2.5p | `ccb-cli gemini 3f "..."` |
| kimi | thinking, normal | `ccb-cli kimi thinking "..."` |
| deepseek | reasoner, chat | `ccb-cli deepseek reasoner "..."` |

---

### Gateway API

**支持 WebSocket 的 RESTful API：**

#### POST /api/ask（同步）

```bash
curl -X POST http://localhost:8765/api/ask \
  -H "Content-Type: application/json" \
  -d '{
    "provider": "kimi",
    "message": "你的问题",
    "wait": true,
    "timeout": 120
  }'

# 响应：
{
  "status": "completed",
  "response": "AI 响应内容...",
  "provider": "kimi",
  "latency_ms": 8500,
  "metadata": {
    "_memory_injected": true,
    "_memory_count": 2,
    "_system_context_injected": true
  }
}
```

#### POST /api/submit（异步）

```bash
# 提交请求
curl -X POST http://localhost:8765/api/submit \
  -d '{"provider": "kimi", "message": "你的问题"}'

# 返回：{"request_id": "abc123", "status": "queued"}

# 查询结果
curl http://localhost:8765/api/query/abc123

# 响应：
{
  "request_id": "abc123",
  "status": "completed",
  "response": "AI 响应...",
  "latency_ms": 8500
}
```

#### WebSocket /ws

```javascript
const ws = new WebSocket('ws://localhost:8765/ws');

ws.onmessage = (event) => {
  const data = JSON.parse(event.data);

  if (data.type === "request_processing") {
    console.log(`处理中: ${data.data.request_id}`);
  } else if (data.type === "request_completed") {
    console.log(`已完成: ${data.data.request_id}`);
  }
};
```

---

## 🧠 记忆系统 (v0.18)

### 架构

记忆系统由三层组成：

1. **系统上下文构建器** - 启动时预加载 Skills/MCP/Providers
2. **记忆中间件** - 注入上下文并记录对话
3. **记忆后端** - SQLite 数据库 + FTS5 搜索

### 数据库结构

```sql
-- conversations 表
CREATE TABLE conversations (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp TEXT NOT NULL,
    provider TEXT NOT NULL,
    question TEXT NOT NULL,
    answer TEXT NOT NULL,
    metadata TEXT,  -- JSON
    tokens INTEGER DEFAULT 0
);

-- FTS5 全文搜索索引
CREATE VIRTUAL TABLE conversations_fts USING fts5(
    question, answer, provider,
    content='conversations',
    content_rowid='id'
);
```

### CLI 命令

```bash
# 查看最近对话
python3 lib/memory/memory_lite.py recent 10

# 搜索对话
python3 lib/memory/memory_lite.py search "React hooks"

# 查看统计
python3 lib/memory/memory_lite.py stats

# 云端同步
ccb-sync push    # 推送到 Google Drive
ccb-sync pull    # 从 Google Drive 拉取
ccb-sync status  # 检查同步状态
```

### 配置

**`~/.ccb/gateway_config.json`：**
```json
{
  "memory": {
    "enabled": true,
    "auto_inject": true,
    "auto_record": true,
    "inject_system_context": true,
    "max_injected_memories": 5
  },
  "recommendation": {
    "enabled": true,
    "auto_switch_provider": false
  }
}
```

---

## 🔍 技能发现 (v0.19)

**自动发现和推荐相关 Claude Code Skills** - 集成 [Vercel Skills](https://github.com/vercel-labs/skills) 按需查找和安装技能。

### 工作原理

```
用户请求 → 提取关键词 → 搜索技能（本地 + 远程）
                                   ↓
                  ┌────────────────┴────────────────┐
                  │                                  │
             scan-skills.sh              npx skills find [query]
             （本地技能）                  （Vercel 仓库）
                  │                                  │
                  └────────────────┬────────────────┘
                                   ↓
                        按相关性得分排序
                     （关键词 + 使用历史）
                                   ↓
                      注入推荐到上下文
                                   ↓
                      AI 看到可用技能
                                   ↓
                  记录使用 → 学习和改进
```

### 功能特性

- 🔍 **本地 + 远程搜索** - 扫描已安装技能并搜索 Vercel Skills 仓库
- 🧠 **学习算法** - 根据使用历史改进推荐
- 📊 **相关性评分** - 关键词 + 历史使用 + 安装状态
- 🚀 **自动安装** - 可选择自动安装推荐的远程技能
- 💾 **缓存结果** - 技能缓存在记忆数据库中以快速访问

### 使用方式

**自动（通过 Gateway）：**
```bash
# Gateway 自动发现相关技能
ccb-cli kimi "帮我测试 React 组件"

# Gateway 输出：
# [MemoryMiddleware] 💡 发现 2 个相关 Skill: /webapp-testing, jest-react-testing
```

**手动搜索：**
```bash
# 查找特定任务的技能
ccb-skills recommend "创建 PDF"

# 输出：
# 💡 发现 1 个相关 Skill: /pdf
#
#   pdf (score: 23, installed: ✓)
#     Comprehensive PDF manipulation toolkit
#     Usage: /pdf

# 查看使用统计
ccb-skills stats

# 刷新缓存
ccb-skills scan
```

### 配置

```json
{
  "skills": {
    "auto_discover": true,        // 自动发现技能
    "recommend_skills": true,     // 显示推荐
    "max_recommendations": 3,     // 最多推荐数量
    "auto_install": false,        // 自动安装远程技能
    "cache_ttl_hours": 24         // 缓存过期时间
  }
}
```

### CLI 命令

| 命令 | 描述 |
|------|------|
| `ccb-skills scan` | 刷新本地技能缓存 |
| `ccb-skills recommend "<task>"` | 获取技能推荐 |
| `ccb-skills match "<task>"` | 查找匹配的技能（详细） |
| `ccb-skills stats` | 显示使用统计 |
| `ccb-skills list [--installed]` | 列出所有/已安装技能 |

### 相关性算法

```python
score = 0

# 名称匹配（最高优先级）
if keyword in skill_name: score += 10

# 描述匹配
if keyword in description: score += 5

# 触发器匹配
if keyword in triggers: score += 3

# 已安装加分
if installed: score += 2

# 使用历史加分（每个关键词最多 +5）
score += min(usage_count, 5)
```

### 示例：自动发现

```bash
# 用户请求
$ ccb-cli kimi "创建一个 Excel 电子表格"

# 幕后处理：
[MemoryMiddleware] 提取的关键词: ['创建', 'excel', '电子表格']
[SkillsDiscovery] 搜索本地技能...
[SkillsDiscovery] 找到: xlsx (score: 20)
[SkillsDiscovery] 搜索远程技能: npx skills find excel
[SkillsDiscovery] 找到: excel-toolkit (score: 15)
[MemoryMiddleware] 💡 发现 2 个相关 Skill: /xlsx, excel-toolkit

# AI 在上下文中看到：
## 🛠️ 相关技能推荐
- **/xlsx** (score: 20) - 全面的电子表格工具包
  ✓ 已安装，可直接使用: `/xlsx`
- **excel-toolkit** (score: 15) - Vercel 的 Excel 自动化工具
  📦 可安装: npx skills add vercel-labs/agent-skills@excel-toolkit -g -y

# 结果：AI 自动使用 /xlsx skill
```

---

## 💬 多 AI 讨论

**多个 AI 协作式问题解决：**

### 基本讨论

```bash
ccb-submit discuss \
  --providers kimi,codex,gemini \
  --rounds 3 \
  --strategy consensus \
  "设计分布式缓存系统"
```

### 聚合策略

- **consensus** - 所有 AI 必须同意
- **majority** - 多数答案获胜
- **first_success** - 第一个有效响应
- **best_quality** - 最高质量（评分）

### 使用场景

```bash
# 架构设计
ccb-submit discuss -p kimi,codex,gemini -r 3 \
  "为电商设计微服务"

# 代码审查
ccb-submit discuss -p codex,deepseek -r 2 \
  "审查此实现：[代码]"

# 头脑风暴
ccb-submit discuss -p kimi,gemini,iflow -r 3 \
  "改进用户引导的想法"
```

---

## 🖥️ Web UI

**实时监控仪表盘：http://localhost:8765/web**

### 功能

- 📊 **实时指标** - 请求数、成功率、延迟
- 📋 **请求队列** - 待处理、处理中、已完成
- 🔴 **实时日志** - 通过 WebSocket 实时事件流
- 🤖 **Provider 状态** - 所有 Providers 的健康检查
- 📈 **图表** - 性能趋势和分析

### 截图

<details>
<summary><b>仪表盘概览</b></summary>

<img src="screenshots/dashboard.png" alt="仪表盘" width="700">

</details>

<details>
<summary><b>请求队列</b></summary>

<img src="screenshots/queue.png" alt="队列" width="700">

</details>

---

## 📖 API 参考

### 端点

| 方法 | 端点 | 描述 |
|--------|----------|-------------|
| GET | `/health` | 健康检查 |
| GET | `/providers` | 列出所有 Providers |
| POST | `/api/ask` | 同步请求 |
| POST | `/api/submit` | 异步请求 |
| GET | `/api/query/{id}` | 查询请求状态 |
| GET | `/api/pending` | 列出待处理请求 |
| POST | `/api/cancel/{id}` | 取消请求 |
| WS | `/ws` | WebSocket 连接 |

### 请求参数

**POST /api/ask & /api/submit：**
```json
{
  "provider": "kimi",           // 必需：AI Provider 名称
  "message": "你的问题",         // 必需：用户消息
  "model": "thinking",          // 可选：特定模型
  "wait": true,                 // 可选：等待完成（仅 ask）
  "timeout": 120,               // 可选：超时秒数
  "metadata": {}                // 可选：自定义元数据
}
```

### 响应格式

```json
{
  "request_id": "abc123",
  "status": "completed",         // queued, processing, completed, failed
  "response": "AI 响应...",      // 仅在 completed 时
  "provider": "kimi",
  "latency_ms": 8500,
  "tokens_used": 150,
  "metadata": {
    "_memory_injected": true,
    "_memory_count": 2,
    "_system_context_injected": true
  },
  "error": null                  // 失败时的错误消息
}
```

---

## 📚 文档资源

### 核心文档

- **[记忆系统架构](lib/memory/INTEGRATION_DESIGN.md)** - 完整设计，包含 4 系统分析
- **[集成报告](lib/memory/INTEGRATION_REPORT.md)** - 完整实施报告
- **[数据库结构](lib/memory/DATABASE_STRUCTURE.md)** - 结构和查询
- **[云端同步指南](lib/memory/SYNC_QUICKSTART.md)** - Google Drive 设置

### 其他资源

- **[API 文档](docs/API.md)** - 完整 API 参考
- **[配置指南](docs/CONFIG.md)** - 所有配置选项
- **[部署指南](docs/DEPLOYMENT.md)** - 生产部署
- **[贡献指南](CONTRIBUTING.md)** - 如何贡献

---

## 🗺️ 开发路线

### v0.19（2026 年 Q2）- 技能发现 ✅

- [x] **技能发现服务** - 自动查找和推荐技能
- [x] **Vercel Skills 集成** - 通过 `npx skills find` 搜索远程技能
- [x] **学习算法** - 基于使用情况改进推荐
- [x] **记忆集成** - 技能缓存和追踪记忆数据库
- [ ] 技能的语义相似度搜索
- [ ] 自动安装热门技能

### v0.20（2026 年 Q3）- 语义增强

- [ ] Qdrant 向量数据库集成
- [ ] 对话的语义相似度搜索
- [ ] LLM 驱动的事实提取
- [ ] 多语言嵌入

### v0.21（2026 年 Q4）- Agent 自主性

- [ ] Agent 记忆函数调用（Letta 模式）
- [ ] 结构化记忆块（core_memory）
- [ ] 自我更新 Agents
- [ ] 记忆版本控制

### v0.22（2026 年 Q4）- 团队协作

- [ ] 多用户记忆隔离
- [ ] 共享记忆池
- [ ] 权限系统
- [ ] 实时协作

---

## 🤝 贡献指南

我们欢迎贡献！详情请查看 [CONTRIBUTING.md](CONTRIBUTING.md)。

### 贡献者快速开始

```bash
# 1. Fork 并克隆
git clone https://github.com/YOUR_USERNAME/ai-router-ccb.git
cd ai-router-ccb

# 2. 创建分支
git checkout -b feature/your-feature

# 3. 修改并测试
python3 -m pytest tests/

# 4. 提交并推送
git commit -m "feat: add your feature"
git push origin feature/your-feature

# 5. 创建 Pull Request
```

### 开发环境设置

```bash
# 安装开发依赖
pip install -r requirements-dev.txt

# 运行测试
pytest

# 运行 linter
flake8 lib/ tests/

# 运行类型检查
mypy lib/
```

---

## 📜 开源许可

本项目采用 MIT 许可证 - 详情请查看 [LICENSE](LICENSE) 文件。

---

## 🙏 致谢

**灵感来源：**
- [Mem0](https://github.com/mem0ai/mem0) - 语义记忆架构
- [Letta (MemGPT)](https://github.com/cpacker/MemGPT) - 结构化记忆块
- [LangChain](https://github.com/langchain-ai/langchain) - 记忆模式
- [claude-mem](https://github.com/thedotmack/claude-mem) - 生命周期钩子

**构建技术：**
- [FastAPI](https://fastapi.tiangolo.com) - 现代 Web 框架
- [SQLite](https://www.sqlite.org) - 可靠数据库
- [Claude Code](https://www.anthropic.com/claude) - AI 编排者

---

## 📞 技术支持

- 📧 邮箱：[your-email@example.com]
- 💬 Discord：[加入社区]
- 🐛 问题：[GitHub Issues](https://github.com/LeoLin990405/ai-router-ccb/issues)
- 📖 文档：[完整文档](https://your-docs-site.com)

---

<div align="center">

**由 CCB 团队用 ❤️ 构建**

**[⬆ 回到顶部](#-ccb-gateway)**

</div>
