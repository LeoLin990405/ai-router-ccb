---
name: pend
description: Fetch latest provider replies via the unified `pend` CLI. Use only when the user explicitly asks.
metadata:
  short-description: Fetch provider replies via pend
---

# pend (Unified)

Use `pend` to fetch provider replies.

## Execution (MANDATORY)

```bash
pend <provider> [N]
```

## Notes

- Use only when the user explicitly requests results.
