---
name: ping
description: Check provider connectivity via the unified `ping` CLI. Use only when the user explicitly asks.
metadata:
  short-description: Ping a provider via ping
---

# ping (Unified)

Use `ping` to check provider connectivity.

## Execution (MANDATORY)

```bash
ping <provider>
```

## Notes

- Use only when the user explicitly asks to check connectivity.
