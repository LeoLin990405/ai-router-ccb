Use `ping` to check provider connectivity via the unified command.

Trigger conditions (ALL must match):
- User explicitly asks to ping/check a provider

Execution:
- `ping <provider>`: `Bash(ping <provider>)`

Output: stdout = health status, exit code 0 = OK
